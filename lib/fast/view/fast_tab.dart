import 'package:flutter/material.dart';

class FastTab extends StatelessWidget {
  const FastTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fast tab"),
    );
  }
}
